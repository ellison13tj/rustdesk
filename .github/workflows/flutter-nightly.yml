name: Flutter Nightly Build

on:
  workflow_dispatch:
  push:
    branches:
      - master

permissions:
  contents: write

jobs:
  run-flutter-nightly-build:
    uses: ./.github/workflows/flutter-build-ubuntu24.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: "nightly"
